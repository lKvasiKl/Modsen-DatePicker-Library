import { COLORS, FONTS, SPACES } from "constants/styles";

import { Theme } from "./types";

export const THEME: Theme = {
  colors: {
    ...COLORS,
  },
  fonts: {
    ...FONTS,
  },
  spaces: {
    ...SPACES,
  },
};
